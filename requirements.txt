# Dépendances minimales du dashboard d'activité RFP / RFI.
# Installation :  pip install -r requirements.txt
#
# Aucune bibliothèque scientifique lourde : les régressions, les p-values et
# les intervalles de confiance sont implémentés dans core.py (SciPy et
# statsmodels ne sont pas nécessaires).

streamlit>=1.31        # interface (app.py)
pandas>=1.5            # manipulation des données
numpy>=1.23            # calcul numérique et algèbre linéaire
plotly>=5.19           # graphiques interactifs (écran et rapport HTML)
openpyxl>=3.1          # lecture des classeurs .xlsx
